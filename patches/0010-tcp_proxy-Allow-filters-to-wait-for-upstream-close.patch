From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Cilium Authors <dev@cilium.io>
Date: Tue, 25 Aug 2026 16:00:00 +0200
Subject: [PATCH] tcp_proxy: Allow filters to wait for peer close

A protocol filter may need either side of a raw TCP proxy connection to remain
open after its local stream has ended so the peer can finish a protocol-level
close handshake. Add per-connection filter-state timeouts which make TCP proxy
flush pending writes and wait for the corresponding peer to close.

The raw TCP connection pool copies the upstream timeout into each TcpUpstream
when it is created, leaving the GenericUpstream extension interface unchanged.
The TCP proxy filter applies the downstream timeout when a raw TCP upstream
closes normally.

Data received from upstream during the final upstream drain interval cannot be
delivered to the closed downstream connection and is discarded. Preserve the
existing close behavior for connections which do not opt in, and always
propagate resets without delay.
---
 source/common/tcp_proxy/BUILD        |  1 +
 source/common/tcp_proxy/tcp_proxy.cc | 28 +++++++++++++++++++++++-----
 source/common/tcp_proxy/tcp_proxy.h  |  8 ++++++--
 source/common/tcp_proxy/upstream.cc  | 25 +++++++++++++++++++++----
 source/common/tcp_proxy/upstream.h   | 14 +++++++++++++-
 test/common/tcp_proxy/tcp_proxy_test.cc | 18 ++++++++++++++++++
 6 files changed, 82 insertions(+), 12 deletions(-)
diff --git a/source/common/tcp_proxy/BUILD b/source/common/tcp_proxy/BUILD
index 914382ab15..41a3f206f4 100644
--- a/source/common/tcp_proxy/BUILD
+++ b/source/common/tcp_proxy/BUILD
@@ -20,6 +20,7 @@ envoy_cc_library(
         "//envoy/http:header_map_interface",
         "//envoy/http:request_id_extension_interface",
         "//envoy/router:router_ratelimit_interface",
+        "//envoy/stream_info:uint64_accessor_interface",
         "//envoy/tcp:conn_pool_interface",
         "//envoy/tcp:upstream_interface",
         "//envoy/upstream:cluster_manager_interface",
diff --git a/source/common/tcp_proxy/tcp_proxy.cc b/source/common/tcp_proxy/tcp_proxy.cc
index e5d23f2199..a984cea223 100644
--- a/source/common/tcp_proxy/tcp_proxy.cc
+++ b/source/common/tcp_proxy/tcp_proxy.cc
@@ -457,6 +457,17 @@ void Filter::initialize(Network::ReadFilterCallbacks& callbacks, bool set_connect
     }
   }
 
+  const auto get_flush_wait_timeout = [this](absl::string_view key) {
+    const StreamInfo::UInt64Accessor* timeout =
+        read_callbacks_->connection()
+            .streamInfo()
+            .filterState()
+            ->getDataReadOnly<StreamInfo::UInt64Accessor>(key);
+    return std::chrono::milliseconds(timeout != nullptr ? timeout->value() : 0);
+  };
+  upstream_flush_wait_timeout_ = get_flush_wait_timeout(UpstreamFlushWaitTimeoutMs);
+  downstream_flush_wait_timeout_ = get_flush_wait_timeout(DownstreamFlushWaitTimeoutMs);
+
   // Handle TLS handshake wait mode.
   if (connect_mode_ == UpstreamConnectMode::ON_DOWNSTREAM_TLS_HANDSHAKE) {
     const auto ssl_connection = read_callbacks_->connection().ssl();
@@ -1204,12 +1215,13 @@ void Filter::onDownstreamEvent(Network::ConnectionEvent event) {
   if (upstream_) {
     absl::string_view downstream_close_details = read_callbacks_->connection().localCloseReason();
     Tcp::ConnectionPool::ConnectionDataPtr conn_data(
         upstream_->onDownstreamEvent(event, downstream_close_details));
     if (conn_data != nullptr &&
         conn_data->connection().state() != Network::Connection::State::Closed) {
       config_->drainManager().add(config_->sharedConfig(), std::move(conn_data),
                                   std::move(upstream_callbacks_), std::move(idle_timer_),
-                                  idle_timeout_, read_callbacks_->upstreamHost());
+                                  idle_timeout_, read_callbacks_->upstreamHost(),
+                                  upstream_flush_wait_timeout_.count() > 0);
     }
     if (event == Network::ConnectionEvent::LocalClose ||
         event == Network::ConnectionEvent::RemoteClose) {
@@ -1286,17 +1298,26 @@ void Filter::onUpstreamEvent(Network::ConnectionEvent event) {
     } else {
       if (read_callbacks_->connection().state() == Network::Connection::State::Open) {
         // Propagate upstream RST to downstream.
         if (upstream_detected_close_type == StreamInfo::DetectedCloseType::RemoteReset &&
             Runtime::runtimeFeatureEnabled("envoy.reloadable_features."
                                            "propagate_upstream_rst_through_tunneled_tcp_proxy")) {
           ENVOY_CONN_LOG(trace, "TCP:onUpstreamEvent(): propagating upstream RST to downstream",
                          read_callbacks_->connection());
           getStreamInfo().setResponseFlag(StreamInfo::CoreResponseFlag::UpstreamRemoteReset);
           read_callbacks_->connection().close(Network::ConnectionCloseType::AbortReset);
         } else {
-          read_callbacks_->connection().close(Network::ConnectionCloseType::FlushWrite);
+          const bool wait_for_peer_close =
+              event == Network::ConnectionEvent::RemoteClose &&
+              upstream_detected_close_type == StreamInfo::DetectedCloseType::Normal &&
+              downstream_flush_wait_timeout_.count() > 0;
+          if (wait_for_peer_close) {
+            read_callbacks_->connection().setDelayedCloseTimeout(downstream_flush_wait_timeout_);
+          }
+          read_callbacks_->connection().close(wait_for_peer_close
+                                                  ? Network::ConnectionCloseType::FlushWriteAndDelay
+                                                  : Network::ConnectionCloseType::FlushWrite);
         }
       }
     }
   }
 }
@@ -1508,8 +1529,10 @@ void UpstreamDrainManager::add(const Config::SharedConfigSharedPtr& config,
                                Event::TimerPtr&& idle_timer,
                                absl::optional<std::chrono::milliseconds> idle_timeout,
-                               const Upstream::HostDescriptionConstSharedPtr& upstream_host) {
+                               const Upstream::HostDescriptionConstSharedPtr& upstream_host,
+                               bool discard_upstream_data) {
   DrainerPtr drainer(new Drainer(*this, config, callbacks, std::move(upstream_conn_data),
-                                 std::move(idle_timer), idle_timeout, upstream_host));
+                                 std::move(idle_timer), idle_timeout, upstream_host,
+                                 discard_upstream_data));
   callbacks->drain(*drainer);
 
   // Use temporary to ensure we get the pointer before we move it out of drainer
@@ -1530,9 +1544,10 @@ Drainer::Drainer(UpstreamDrainManager& parent, const Config::SharedConfigSharedPt
                  Tcp::ConnectionPool::ConnectionDataPtr&& conn_data, Event::TimerPtr&& idle_timer,
                  absl::optional<std::chrono::milliseconds> idle_timeout,
-                 const Upstream::HostDescriptionConstSharedPtr& upstream_host)
+                 const Upstream::HostDescriptionConstSharedPtr& upstream_host,
+                 bool discard_upstream_data)
     : parent_(parent), callbacks_(callbacks), upstream_conn_data_(std::move(conn_data)),
       idle_timer_(std::move(idle_timer)), idle_timeout_(idle_timeout),
-      upstream_host_(upstream_host), config_(config) {
+      upstream_host_(upstream_host), config_(config), discard_upstream_data_(discard_upstream_data) {
   ENVOY_CONN_LOG(trace, "draining the upstream connection", upstream_conn_data_->connection());
   config_->stats().upstream_flush_total_.inc();
   config_->stats().upstream_flush_active_.inc();
@@ -1551,6 +1564,10 @@ void Drainer::onEvent(Network::ConnectionEvent event) {
 }
 
 void Drainer::onData(Buffer::Instance& data, bool) {
+  if (discard_upstream_data_) {
+    data.drain(data.length());
+    return;
+  }
   if (data.length() > 0) {
     // There is no downstream connection to send any data to, but the upstream
     // sent some data. Try to behave similar to what the kernel would do
diff --git a/source/common/tcp_proxy/tcp_proxy.h b/source/common/tcp_proxy/tcp_proxy.h
index b10971523a..8a6f53b70c 100644
--- a/source/common/tcp_proxy/tcp_proxy.h
+++ b/source/common/tcp_proxy/tcp_proxy.h
@@ -746,4 +755,6 @@ protected:
   absl::optional<std::chrono::milliseconds> idle_timeout_;
+  std::chrono::milliseconds upstream_flush_wait_timeout_{0};
+  std::chrono::milliseconds downstream_flush_wait_timeout_{0};
   uint32_t connect_attempts_{};
   bool connecting_{};
   bool downstream_closed_{};
@@ -778,6 +788,7 @@ public:
           Tcp::ConnectionPool::ConnectionDataPtr&& conn_data, Event::TimerPtr&& idle_timer,
           absl::optional<std::chrono::milliseconds> idle_timeout,
-          const Upstream::HostDescriptionConstSharedPtr& upstream_host);
+          const Upstream::HostDescriptionConstSharedPtr& upstream_host,
+          bool discard_upstream_data);
 
   void onEvent(Network::ConnectionEvent event);
   void onData(Buffer::Instance& data, bool end_stream);
@@ -794,5 +805,6 @@ private:
   Upstream::HostDescriptionConstSharedPtr upstream_host_;
   Config::SharedConfigSharedPtr config_;
+  const bool discard_upstream_data_;
 };
 
 using DrainerPtr = std::unique_ptr<Drainer>;
@@ -806,6 +818,7 @@ public:
            const std::shared_ptr<Filter::UpstreamCallbacks>& callbacks,
            Event::TimerPtr&& idle_timer, absl::optional<std::chrono::milliseconds> idle_timeout,
-           const Upstream::HostDescriptionConstSharedPtr& upstream_host);
+           const Upstream::HostDescriptionConstSharedPtr& upstream_host,
+           bool discard_upstream_data);
   void remove(Drainer& drainer, Event::Dispatcher& dispatcher);
 
 private:
diff --git a/test/common/tcp_proxy/tcp_proxy_test.cc b/test/common/tcp_proxy/tcp_proxy_test.cc
index e11fdfbef3..0000000000 100644
--- a/test/common/tcp_proxy/tcp_proxy_test.cc
+++ b/test/common/tcp_proxy/tcp_proxy_test.cc
@@ -373,3 +373,21 @@ TEST_P(TcpProxyTest, UpstreamRemoteDisconnect) {
+TEST_P(TcpProxyTest, UpstreamRemoteDisconnectWaitsForDownstreamPeer) {
+  constexpr uint64_t timeout_ms = 1000;
+  filter_callbacks_.connection_.streamInfo().filterState()->setData(
+      DownstreamFlushWaitTimeoutMs,
+      std::make_unique<StreamInfo::UInt64AccessorImpl>(timeout_ms),
+      StreamInfo::FilterState::StateType::ReadOnly,
+      StreamInfo::FilterState::LifeSpan::Connection);
+
+  setup(1);
+  raiseEventUpstreamConnected(0);
+
+  EXPECT_CALL(filter_callbacks_.connection_,
+              setDelayedCloseTimeout(std::chrono::milliseconds(timeout_ms)));
+  EXPECT_CALL(filter_callbacks_.connection_,
+              close(Network::ConnectionCloseType::FlushWriteAndDelay));
+  upstream_callbacks_->onEvent(Network::ConnectionEvent::RemoteClose);
+}
+
 // Test that upstream RemoteClose propagates AbortReset (default behavior)
 // when upstream detected close type is RemoteReset.
 TEST_P(TcpProxyTest, UpstreamRemoteCloseWithRstPropagation) {
diff --git a/source/common/tcp_proxy/upstream.cc b/source/common/tcp_proxy/upstream.cc
index f466703e25..c39ed0c333 100644
--- a/source/common/tcp_proxy/upstream.cc
+++ b/source/common/tcp_proxy/upstream.cc
@@ -3,4 +3,5 @@
 #include "envoy/http/header_map.h"
+#include "envoy/stream_info/uint64_accessor.h"
 #include "envoy/upstream/cluster_manager.h"
 
 #include "source/common/http/codec_client.h"
@@ -66,8 +67,10 @@ void generateAndStoreRequestId(const TunnelingConfigHelper& config, Http::Request
 
 TcpUpstream::TcpUpstream(Tcp::ConnectionPool::ConnectionDataPtr&& data,
-                         Tcp::ConnectionPool::UpstreamCallbacks& upstream_callbacks)
-    : upstream_conn_data_(std::move(data)) {
+                         Tcp::ConnectionPool::UpstreamCallbacks& upstream_callbacks,
+                         std::chrono::milliseconds upstream_flush_wait_timeout)
+    : upstream_conn_data_(std::move(data)),
+      upstream_flush_wait_timeout_(upstream_flush_wait_timeout) {
   Network::ClientConnection& connection = upstream_conn_data_->connection();
   connection.enableHalfClose(true);
   upstream_conn_data_->addUpstreamCallbacks(upstream_callbacks);
 }
@@ -125,14 +128,19 @@ StreamInfo::DetectedCloseType TcpUpstream::detectedCloseType() const {
 }
 
 Tcp::ConnectionPool::ConnectionData* TcpUpstream::onDownstreamEvent(Network::ConnectionEvent event,
                                                                     absl::string_view details) {
   // TODO(botengyao): propagate RST back to upstream connection if RST is received from downstream.
   if (event == Network::ConnectionEvent::RemoteClose) {
     // The close call may result in this object being deleted. Latch the
     // connection locally so it can be returned for potential draining.
     auto* conn_data = upstream_conn_data_.release();
+    const bool wait_for_peer_close = upstream_flush_wait_timeout_.count() > 0;
+    if (wait_for_peer_close) {
+      conn_data->connection().setDelayedCloseTimeout(upstream_flush_wait_timeout_);
+    }
     conn_data->connection().close(
-        Network::ConnectionCloseType::FlushWrite,
+        wait_for_peer_close ? Network::ConnectionCloseType::FlushWriteAndDelay
+                            : Network::ConnectionCloseType::FlushWrite,
         StreamInfo::LocalCloseReasons::get().ClosingUpstreamTcpDueToDownstreamRemoteClose);
     return conn_data;
   } else if (event == Network::ConnectionEvent::LocalClose) {
@@ -365,7 +373,16 @@ void TcpConnPool::onPoolReady(Tcp::ConnectionPool::ConnectionDataPtr&& conn_data,
   Tcp::ConnectionPool::ConnectionData* latched_data = conn_data.get();
   Network::Connection& connection = conn_data->connection();
 
-  auto upstream = std::make_unique<TcpUpstream>(std::move(conn_data), upstream_callbacks_);
+  std::chrono::milliseconds upstream_flush_wait_timeout{0};
+  const StreamInfo::UInt64Accessor* flush_wait_timeout =
+      downstream_info_.filterState()->getDataReadOnly<StreamInfo::UInt64Accessor>(
+          UpstreamFlushWaitTimeoutMs);
+  if (flush_wait_timeout != nullptr) {
+    upstream_flush_wait_timeout = std::chrono::milliseconds(flush_wait_timeout->value());
+  }
+
+  auto upstream = std::make_unique<TcpUpstream>(std::move(conn_data), upstream_callbacks_,
+                                                upstream_flush_wait_timeout);
   callbacks_->onGenericPoolReady(
       &connection.streamInfo(), std::move(upstream), host,
       latched_data->connection().connectionInfoProvider(),
diff --git a/source/common/tcp_proxy/upstream.h b/source/common/tcp_proxy/upstream.h
index 4ce796b563..28118fe62b 100644
--- a/source/common/tcp_proxy/upstream.h
+++ b/source/common/tcp_proxy/upstream.h
@@ -1,5 +1,7 @@
 #pragma once
 
+#include <chrono>
+
 #include <memory>
 
 #include "envoy/http/conn_pool.h"
@@ -28,6 +30,21 @@ namespace Envoy {
 namespace TcpProxy {
 
 constexpr absl::string_view DisableTunnelingFilterStateKey = "envoy.tcp_proxy.disable_tunneling";
+/**
+ * UpstreamFlushWaitTimeoutMs is the key for a per-connection upstream flush wait timeout. A
+ * non-zero ``StreamInfo::UInt64Accessor`` value makes a raw TCP upstream wait for its peer to
+ * close after pending writes have been flushed when the downstream connection closes. Data
+ * received during this interval is discarded because the downstream connection no longer exists.
+ */
+constexpr absl::string_view UpstreamFlushWaitTimeoutMs =
+    "envoy.tcp_proxy.upstream_flush_wait_timeout_ms";
+/**
+ * DownstreamFlushWaitTimeoutMs is the corresponding downstream flush wait timeout. A non-zero
+ * ``StreamInfo::UInt64Accessor`` value makes the downstream connection wait for its peer to close
+ * after pending writes have been flushed when the raw TCP upstream closes normally.
+ */
+constexpr absl::string_view DownstreamFlushWaitTimeoutMs =
+    "envoy.tcp_proxy.downstream_flush_wait_timeout_ms";
 
 class TcpConnPool : public GenericConnPool, public Tcp::ConnectionPool::Callbacks {
 public:
@@ -172,6 +182,7 @@ class TcpUpstream : public GenericUpstream {
 public:
   TcpUpstream(Tcp::ConnectionPool::ConnectionDataPtr&& data,
-              Tcp::ConnectionPool::UpstreamCallbacks& callbacks);
+              Tcp::ConnectionPool::UpstreamCallbacks& callbacks,
+              std::chrono::milliseconds upstream_flush_wait_timeout = {});
 
   // GenericUpstream
   bool readDisable(bool disable) override;
@@ -186,6 +197,7 @@ public:
 
 private:
   Tcp::ConnectionPool::ConnectionDataPtr upstream_conn_data_;
+  const std::chrono::milliseconds upstream_flush_wait_timeout_;
 };
 
 class HttpUpstream : public GenericUpstream, protected Http::StreamCallbacks {
